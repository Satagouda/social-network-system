server:
  port: 8083

spring:
  application:
    name: FEED-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/social_db

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}